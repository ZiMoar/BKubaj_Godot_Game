class_name NarrowArenaMap
extends MapBase

## Map data for the "Narrow Arena" — a tall, slim arena.
## The wall lengths are swapped relative to the test arena (portrait instead of
## landscape), forcing tighter vertical play.